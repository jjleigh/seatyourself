// $('#review-list').last().css("background color", "yellow");
$(window).scroll(function() {
	var scrollTop = $(window).scrollTop();
	if (scrollTop > $('#review-list').offset().top) {

	}
})

var com = function() {
	$('#review-list').(function() {
		$(this).children()
})
}

